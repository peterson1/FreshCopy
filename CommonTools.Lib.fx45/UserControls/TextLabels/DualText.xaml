<UserControl x:Class="CommonTools.Lib.fx45.UserControls.TextLabels.DualText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommonTools.Lib.fx45.UserControls.TextLabels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="colDef1" />
            <ColumnDefinition x:Name="colDefGap" />
            <ColumnDefinition x:Name="colDef2" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="txt1"
                   Grid.Column="0"
                   VerticalAlignment="Bottom"
                   />

        <Rectangle Grid.Column="1" />

        <TextBlock x:Name="txt2"
                   Grid.Column="2"
                   VerticalAlignment="Bottom"
                   />

        <TextBlock x:Name="elp2"
                   Text=" ..."
                   Grid.Column="3"
                   VerticalAlignment="Bottom"
                   />
    </Grid>
</UserControl>
