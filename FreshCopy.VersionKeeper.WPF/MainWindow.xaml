<Window x:Class="FreshCopy.VersionKeeper.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FreshCopy.VersionKeeper.WPF"
        mc:Ignorable="d"
        Title="{Binding Caption}" 
        Height="350" Width="525">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding TestSend1Cmd}" Content="Test Send 1" />
            <Button Command="{Binding TestSend2Cmd}" Content="Test Send 2" />
        </StackPanel>
        <DataGrid DataContext="{Binding Clients}"
                  ItemsSource="{Binding List}"
                  />
        <ListBox DataContext="{Binding CommonLogs}"
                 ItemsSource="{Binding List}"
                 />
    </StackPanel>
</Window>
